<div class="challenge1">
    <div class="welcome">
        <h1>Contact Us</h1>
        <p>Welcome, we are eager to serve you.</p>
    </div>
    <form class="myForm" id="form">
        <ul class="form-row">
            <li>
                <label for="first-name">First Name</label>
                <input type="text" id="first-name" required>
            </li>
            <li>
                <label for="last-name">Last Name<span class="required">*</span></label>
                <input type="text" id="last-name" required>
            </li>
            <li>
                <label for="phone">Phone<span class="required">*</span></label>
                <input type="tel" name="phone" id="intTextBox" placeholder="" aria-required="false" pattern="^[+]?([(][0-9]{1,3}[)][-]?)?([0-9][-]?){3,16}[0-9]$"
                    maxlength="10" aria-invalid="true" required>
            </li>
            <li>
                <label for="email">Email<span class="required">*</span></label>
                <input type="email" id="email" required>
            </li>
            <li>
                <label for="symptom">What is your greatest struggle today?</label>
                <select name="symptom" id="symptom">
                    <option value="choose">Please choose one</option>
                    <option value="ADHD">ADHD/ADD</option>
                    <option value="Adoption">Adoption</option>
                    <option value="angerManagement">Anger management</option>
                    <option value="relationship">Relationship problems</option>
                    <option value="Bipolar">Bipolar</option>
                    <option value="Depression">Depression</option>
                    <option value="GriefLoss">Grief and Loss</option>
                    <option value="OCD">OCD</option>
                    <option value="Parenting">Parenting</option>
                    <option value="SelfWorth">Self Worth</option>
                    <option value="ConflictManagement">Conflict Management</option>
                    <option value="SubstanceAbuse">Substance Abuse</option>
                    <option value="Addictions">Addictions</option>
                    <option value="TraumaPTSD">Trauma PTSD</option>
                    <option value="WorkRelatedStress">Work Related Stress</option>
                    <option value="other">Other</option>
                </select>
            </li>
            <li>
                <input type="submit" class="submit-button" id="submit-button" value="Submit" required>
            </li>
        </ul>
    </form>
</div>

<style>
    :root {
        --margin-bottom: 10px;
        --label-padding-right: 20px;
        --label-width: 200px;
        --widget-width: 250px;
        --border-radius: 4px;
        --main-padding: 30px;
        --input-text-size: 14px;
        --color-main: #5D6063;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        color: var(--color-main);
        background-color: rgb(182, 219, 255);
        font-family: Arial, Helvetica, sans-serif;
        line-height: 1.5;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .welcome {
        text-align: center;
        background-color: rgb(230, 230, 230);
        border: 1px solid rgb(186, 189, 190);
        border-radius: var(--border-radius);
        box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.5);
        margin: 25px 0;
        padding: var(--main-padding);
    }

    h1 {
        font-weight: bold;
        font-size: 20px;
    }

    .profile-form {
        background-color: rgb(241, 238, 238);
        border: 1px solid rgb(186, 189, 190);
        border-radius: var(--border-radius);
        box-shadow: 0 1px 2px rgb(0, 0, 0, 0.5);
        padding: var(--main-padding);
        width: 600px;
    }

    .form-row {
        display: flex;
        flex-wrap: wrap;
        align-items: flex-start;
    }

    .form-row input[type="text"],
    .form-row input[type="email"],
    .form-row input[type="tel"] {
        border-radius: var(--border-radius);
        color: var(--color-main);
        font-size: var(--input-text-size);
        background-color: rgb(255, 255, 255);
        border: 1px solid rgb(186, 189, 190);
        padding: 7px;
        width: var(--widget-width);
    }

    .myForm {
        width: 600px;
    }

    .form-row input[type="email"]:invalid {
        border: 1.5px solid rgb(212, 53, 56);
        color: rgb(212, 53, 56);
        box-shadow: none;
    }

    .form-row label {
        margin-bottom: 20px;
        padding-right: var(--label-padding-right);
        text-align: right;
        margin-top: 5px;
    }

    .welcome,
    .profile-form {
        width: 600px;
    }

    fieldset {
        text-align: center;
        width: 50%;
        margin: 0 auto;
    }

    .form-row select {
        padding: 5px;
        font-size: 14px;
        width: var(--widget-width);
    }

    .submit-button {
        background: rgb(231, 2, 2)!important;
        justify-self: center;
        justify-content: center;
        font-size: 1em;
        font-weight: bold;
        color: #ffffff;
        border: none;
        border-radius: var(--border-radius);
        padding: 10px 40px;
        cursor: pointer;
        margin-left: var(--label-width);
    }

    .submit-button:hover {
        background-color: rgb(255, 30, 30);
    }

    li {
        padding-bottom: 1rem;
        padding-top: 1rem;
    }
</style>

<script>
    console.log('start')
    // Restricts input for the given textbox to the given inputFilter.
    function setInputFilter(textbox, inputFilter) {
        ["input", "keydown", "keyup", "mousedown", "mouseup", "select", "contextmenu", "drop"].forEach(function (event) {
            textbox.addEventListener(event, function () {
                if (inputFilter(this.value)) {
                    this.oldValue = this.value;
                    this.oldSelectionStart = this.selectionStart;
                    this.oldSelectionEnd = this.selectionEnd;
                } else if (this.hasOwnProperty("oldValue")) {
                    this.value = this.oldValue;
                    this.setSelectionRange(this.oldSelectionStart, this.oldSelectionEnd);
                } else {
                    this.value = "";
                }
            });
        });
    }
    console.log('textinput')
    // Install input filters.
    setInputFilter(document.getElementById("intTextBox"), function (value) {
        return /^-?\d*$/.test(value);
    }); 
    
    var form = document.getElementById('myForm');  // Corrected ID name
    
    var form = document.getElementById('form');

form.addEventListener('submit', function(event) {
  event.preventDefault();

  var firstName = document.getElementById('first-name').value;
  console.log(firstName);

  var lastName = document.getElementById('last-name').value;
  console.log(lastName);

  var phone = document.getElementById('intTextBox').value;
  console.log(phone);

  var email = document.getElementById('email').value;
  console.log(email);

  var symptoms = document.getElementById('symptom').value;
  console.log(symptoms);
});
</script>
